# step-to-openscad
Exploring the possibility of converting step files to openscad files

# Progress

Currently, we only could achieve step -> stl -> scad.

What that means is that we lose faces, edges, and vertices. 

Wouldn't consider a full failure, but a lesson learned
